/*
package recursion;

public class FiboRecurision {
    public static void main(String[] args) {
        int n=2;
        int ans=fib(n);
        System.out.println(ans);
        int result=fibiteration(5);
        System.out.println(result);
    }
    static int fib(int n)
    {
        if(n<2)
        {
            return n;
        }
        else {
            return fib(n-1)+(n-2);
        }
    }
    static int fibiteration(int n)
    {
        int first=0;
        int second=1;
        int ans=first+second;
        if(n<1)
        {
            return first;
        }
        if(n > 0 && n < 2)
        {
            return second;
        }
        for(int i=2; i <= n; i++)
        {
            //return fi
        }
    }
}
*/
