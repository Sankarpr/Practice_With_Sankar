package com.basics;
/*
An interface is a blue print of  class
interface contains final and static variables
it contains abstarct method (also allowed default methods and static methods from java8 onwards
An abstract method is a method contains signature but not body (Un implemented method)
Methods in interface are public
interface supports functionality of multiple inheritence
we can define interface with keyword interface
A class extends another class an interface extends another interface but a class can implements an interface
We can create object reference for interface but we cannot instantiate intereface

 */

public class InterfaceUse {
}
